# Port targets — `aws-sigv4-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `aws-sigv4` that codex-rs uses, and therefore the minimum subset that `aws-sigv4-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-21] (rank `165`, tier `direct`, status `To Do`)

**Cargo package name**: `aws-sigv4`
**Rust module name (in `use` statements)**: `aws_sigv4`

## Summary

- **5** distinct symbols imported by codex-rs
- **5** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `aws_sigv4::http_request::SignableBody`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/signing.rs:5` (`codex-aws-auth`) — `use aws_sigv4::http_request::SignableBody`

### `aws_sigv4::http_request::SignableRequest`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/signing.rs:6` (`codex-aws-auth`) — `use aws_sigv4::http_request::SignableRequest`

### `aws_sigv4::http_request::SigningSettings`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/signing.rs:7` (`codex-aws-auth`) — `use aws_sigv4::http_request::SigningSettings`

### `aws_sigv4::http_request::sign`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/signing.rs:8` (`codex-aws-auth`) — `use aws_sigv4::http_request::sign`

### `aws_sigv4::sign::v4`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-aws-auth`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `aws-auth/src/signing.rs:9` (`codex-aws-auth`) — `use aws_sigv4::sign::v4`
